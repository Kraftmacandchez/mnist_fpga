Currently this project works using the python script that runs through jtag but not the one that runs through uart. I am still working on the uart part but if you want to see it working with the jtag go for it.
Make sure you use the clean_build project for Vivado because somehow my mnist_fpga project in Vivado activated DFX which made me remake the entire block design into clean build.
